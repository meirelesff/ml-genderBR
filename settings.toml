# Mongodb
mongo_add = "localhost"

# Train data 
sample_size = "all" # "all" to use all the data

# Models
seed = 222 # Reprodutible seed
splits = 5 # K folds in the data
repeats = 5 # Number of cvs
threads = -1 # -1 to use all available cores
n_iter = 50 # Number of random draws used in randomgridsearch

# LSTM model config
pad = 20 # Max character length used to truncate names
epochs = 2
batch_size = 128